import React from 'react';
import './Pin.scss';

class Pin extends React.Component {
  render() {
    return (
      <div className="Pin">
        ima pin not a pig
      </div>
    );
  }
}

export default Pin;
